using System;
using Dwares.Dwarf;
using Dwares.Druid;


namespace Passket.ViewModels
{
	public class FavoriteListViewModel : ViewModel
	{
		//static ClassRef @class = new ClassRef(typeof(FavoriteListViewModel));

		public FavoriteListViewModel()
		{
			//Debug.EnableTracing(@class);

			Title = "Favorite";
		}
	}
}
