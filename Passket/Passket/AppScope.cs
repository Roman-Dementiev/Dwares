using System;
using Dwares.Dwarf;


namespace Passket
{
	public class AppScope
	{
		//static ClassRef @class = new ClassRef(typeof(AppScope));

		public AppScope()
		{
			//Debug.EnableTracing(@class);
		}
	}
}
