<UserControl
	x:Class="AssetWerks.IconsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:AssetWerks"
	xmlns:model="using:AssetWerks.Model"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	d:DesignHeight="300"
	d:DesignWidth="400">

	<UserControl.Resources>
		<CollectionViewSource x:Name="IconsCVS"
							  IsSourceGrouped="False"
							  Source="{x:Bind ViewModel.IconGroups}"
							  />

		<DataTemplate x:Name="IconGroupTemplate" x:DataType="model:IconGroup">
			<StackPanel Orientation="Vertical" 
						HorizontalAlignment="Stretch">
				<CheckBox Content="{x:Bind Title}"/>
				<VariableSizedWrapGrid x:Name="wrapGrid" 
									   Orientation="Horizontal"
									   Height="100"
									   >
					<local:IconControl Width="40" Height="40" />
					<local:IconControl Width="80" Height="80" />
				</VariableSizedWrapGrid>
			</StackPanel>
		</DataTemplate>
	</UserControl.Resources>

	<Frame BorderBrush="Black" BorderThickness="1"> 
		<ListView x:Name="listView"
				  ItemsSource="{x:Bind IconsCVS.View}"
				  ItemTemplate="{StaticResource IconGroupTemplate}"
				  HorizontalAlignment="Stretch"
				  SelectionMode="None"
				  >
		</ListView>
	</Frame>
</UserControl>
